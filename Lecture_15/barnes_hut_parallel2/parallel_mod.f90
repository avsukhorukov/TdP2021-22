! In this solution the tree is partitioned into four parts at the first level.
module parallel_mod

contains
    !---------------------------------------------------------------------------

end module parallel_mod